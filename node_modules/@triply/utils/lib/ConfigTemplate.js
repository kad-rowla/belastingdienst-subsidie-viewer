import { merge } from "lodash-es";
import { getEnvironmentVarsAsObject } from "./configUtils.js";
class Config {
    envVarPrefix = "TRIPLY__";
    isDevelopment = process.env.NODE_ENV !== "production";
    constructor(envVarPrefix) {
        if (envVarPrefix)
            this.envVarPrefix = envVarPrefix;
        this.setFromEnvironment();
        this.setDefaults();
        this.setFromEnvironment();
        this.postProcess();
    }
    setFromEnvironment() {
        merge(this, getEnvironmentVarsAsObject({ envPrefix: this.envVarPrefix, env: process.env }));
    }
}
export default Config;
//# sourceMappingURL=ConfigTemplate.js.map