import * as N3 from "n3";
export function serializeTurtleFamily(quads, format = "application/n-quads") {
    return new Promise((resolve, reject) => {
        const writer = new N3.Writer({ format });
        writer.addQuads(quads);
        writer.end(function (error, result) {
            if (error)
                return reject(error);
            resolve(result);
        });
    });
}
//# sourceMappingURL=turtleFamily.js.map