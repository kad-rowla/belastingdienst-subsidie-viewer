import { AssertionError } from "assert";
export function assertIsDefined(val, msg) {
    if (val === undefined || val === null) {
        throw new AssertionError({ message: msg || `Expected value to be defined, but received ${typeof val}` });
    }
}
export function assertIsString(val, msg) {
    if (typeof val !== "string") {
        throw new AssertionError({ message: msg || `Expected value to be a string, but received ${typeof val}` });
    }
}
export function assertIsError(e) {
    if (!(e instanceof Error)) {
        throw new AssertionError({ message: `Expected value to be instance of the Error object.` });
    }
}
//# sourceMappingURL=asserts.js.map