export function getElasticSimpleSearchQueryObject(searchString, from) {
    const query = {
        query: {
            bool: {
                must: {
                    simple_query_string: {
                        query: searchString,
                    },
                },
                should: {
                    simple_query_string: {
                        query: searchString,
                        fields: ["http://www w3 org/2000/01/rdf-schema#label"],
                    },
                },
            },
        },
    };
    if (from !== undefined) {
        query.from = from;
    }
    return query;
}
//# sourceMappingURL=elasticUtils.js.map