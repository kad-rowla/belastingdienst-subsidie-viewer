/// <reference types="node" resolution-mode="require"/>
import { Models } from "@triply/utils";
import Dataset from "./Dataset.js";
export default class Asset {
    private _info;
    private _app;
    private _dataset;
    private _deleted;
    private _selectedVersion?;
    readonly type = "Asset";
    constructor(dataset: Dataset, info: Models.Asset, selectedVersion?: number);
    private _getUrl;
    toFile(destinationPath: string, versionNumber?: number): Promise<void>;
    toStream(versionNumber?: number): Promise<NodeJS.WriteStream>;
    private _getLastVersionInfo;
    getVersionInfo(versionNumber: number): Models.AssetVersion;
    getInfo(versionNumber?: number): Models.Asset & Omit<Models.AssetVersion, "id">;
    private refreshInfo;
    selectVersion(versionNumber: number): this;
    addVersion(fileOrPath: File | string): Promise<this>;
    delete(versionNumber?: number): Promise<this>;
    private static uploadAsset;
}
//# sourceMappingURL=Asset.d.ts.map