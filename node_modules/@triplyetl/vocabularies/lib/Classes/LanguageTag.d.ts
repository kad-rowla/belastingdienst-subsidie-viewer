import { type Tag } from 'language-tags';
import type Region from './Region.js';
export default class LanguageTag<T extends string = string> {
    protected $tag: Tag;
    constructor(tag: T);
    toString(): string;
    static isLanguageTag(obj: any): obj is LanguageTag;
    addRegion<R extends string = string>(region: Region<R>): LanguageTag<`${T}-${R}`>;
}
//# sourceMappingURL=LanguageTag.d.ts.map