import tags from 'language-tags';
export default class LanguageTag {
    $tag;
    constructor(tag) {
        if (!tags.check(tag)) {
            throw new Error(`Tag '${tag}' is not a valid language tag.`);
        }
        this.$tag = tags(tag);
    }
    toString() {
        return this.$tag.format();
    }
    static isLanguageTag(obj) {
        return obj !== undefined && typeof obj === 'object' && typeof obj.addRegion === 'function';
    }
    addRegion(region) {
        return new LanguageTag((this.$tag.format() + '-' + region.toString()));
    }
}
//# sourceMappingURL=LanguageTag.js.map