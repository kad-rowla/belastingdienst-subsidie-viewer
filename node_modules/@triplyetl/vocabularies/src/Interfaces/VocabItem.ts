const categories = ['property', 'class', 'unit of measure', 'datatype', 'instance'] as const
export type Category = (typeof categories)[number]

export interface VocabItem {
  category: Category
  label?: string
  parent?: string
  domain?: string
  range?: string
  description?: string
}
