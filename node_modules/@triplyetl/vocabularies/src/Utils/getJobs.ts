import type Job from '../Interfaces/Job.js'
import { readFileSync } from 'fs'
import path from 'path'
export default (): Job[] => JSON.parse(readFileSync(path.join(process.cwd(), 'vocabularies.json'), 'utf-8'))
