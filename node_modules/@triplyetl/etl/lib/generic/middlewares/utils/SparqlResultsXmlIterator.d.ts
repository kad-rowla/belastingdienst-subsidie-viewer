import { Etl } from "../../index.js";
import { QueryInfo, QuerySourceGetter } from "../../locations/sources/savedQuery.js";
export declare class SparqlResultsXmlIterator implements AsyncIterator<string>, AsyncIterable<string> {
    private app;
    private query;
    private nextLink?;
    private firstLink;
    private $queryInfo?;
    private runs;
    constructor(app: Etl, query: QuerySourceGetter);
    protected queryInfo(): Promise<QueryInfo & {
        headers?: Record<string, string> | undefined;
    }>;
    private fetch;
    next(): Promise<{
        value: string;
        done: boolean;
    }>;
    [Symbol.asyncIterator](): this;
    private parseLink;
}
//# sourceMappingURL=SparqlResultsXmlIterator.d.ts.map