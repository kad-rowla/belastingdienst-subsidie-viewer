import { Environment } from "./index.js";
export type Record = {
    $recordId: number;
    $fileName?: string;
    $sheetName?: string;
    $selector?: string;
    $tagName?: string;
    $environment: Environment;
    [key: string]: unknown;
};
export declare const key: {
    /**
     * The name of the source (e.g. TriplyDB asset) that contains the current
     * Etl record.
     */
    fileName: string;
    /**
     * The mode in which the etl is run.  See the `Mode` type.
     */
    environment: string;
    /**
     * For list-shaped source data: a link to the context that directly
     * contains the list in which the current Etl record appears as a element.
     */
    parent: string;
    /**
     * The numeric identifier of the current Etl record.
     */
    recordId: string;
    /**
     * For tree-shaped source data: the full tree object.
     */
    root: string;
    /**
     * For tree-shaped source data: the path through which the current Etl
     * record was selected.
     */
    selector: string;
    /**
     * For tree-shaped source data: the last tag in the path through which the
     * current Etl record was selected.  This tag often indicates what kind of
     * record is obtained.
     */
    tagName: string;
    /**
     * For Xlsx (Excel) source data: thename of the sheet from which the
     * current Etl record was selected.
     */
    sheetName: string;
};
//# sourceMappingURL=Record.d.ts.map