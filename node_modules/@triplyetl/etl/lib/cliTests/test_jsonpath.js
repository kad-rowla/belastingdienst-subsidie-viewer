import { Etl, fromJson } from "../generic/index.js";
import { iri, jpath, literal, triple } from "../ratt/index.js";
import { sdo, xsd } from "@triplyetl/vocabularies";
import { logQuads } from "../debug/index.js";
const data = {
    fields: [
        {
            id: "1029",
            value: "21",
        },
        {
            id: "1030",
            value: "1",
        },
    ],
};
export default () => {
    const app = new Etl();
    app.use(fromJson(data), triple(iri(app.standardPrefixes.id, "$recordId"), sdo.numberOfAirbags, literal(jpath('$.fields[?(@.id=="1030")].value'), xsd.integer)), logQuads());
    return app;
};
//# sourceMappingURL=test_jsonpath.js.map