import { expect } from "chai";
export function expectNoBlankNodes(store) {
    for (const quad of store.getQuads({})) {
        for (const position of ["subject", "predicate", "object", "graph"]) {
            expect(quad[position].termType).not.to.equal("BlankNode");
        }
    }
}
export const expectNoBlankNodesMw = async function expectNoBlankNodesMw(ctx, next) {
    expectNoBlankNodes(ctx.store);
    return next();
};
//# sourceMappingURL=util.js.map