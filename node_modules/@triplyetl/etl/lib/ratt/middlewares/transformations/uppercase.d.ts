import { Middleware } from "../../../generic/index.js";
import { Key } from "../../../generic/Context.js";
import { StaticString } from "../assertions/term/str.js";
/**
 * Translates a string value to its uppercase variant.
 *
 * @remarks
 * This middleware can uppercase strings in any language; the Unicode Default
 * Case Conversion algorithm is used for this.
 *
 * @param args.content - A key that contains a string value.
 *
 * @param args.key - A new key where the uppercase variant is stored.
 *
 * @example
 * We do not have a good example for this transformation middleware yet.  Let
 * us know in case you have a good example!
 */
declare const _default: (args: {
    content: StaticString | Key;
    key: string;
}) => Middleware;
export default _default;
//# sourceMappingURL=uppercase.d.ts.map