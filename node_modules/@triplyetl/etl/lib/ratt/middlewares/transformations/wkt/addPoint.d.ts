import { Key, Middleware } from "../../../../generic/index.js";
import { PossibleIri } from "../../../../utils/index.js";
import { StaticString } from "../../assertions/term/str.js";
/**
 * Adds the Well-Known Text representation of the geospatial point
 * represented by the dynamic latitude and longitude values.
 * Optionally, a dynamic or static CRS can also be specified.
 */
export default function <R = unknown>(args: {
    crs?: PossibleIri | Key;
    latitude: StaticString | Key;
    longitude: StaticString | Key;
    key: string;
}): Middleware<R>;
//# sourceMappingURL=addPoint.d.ts.map