import { expect } from "chai";
import { isNodejs, safeStringify } from "../index.js";
describe("Generic utils", function () {
    it("Should report node runtime", async function () {
        expect(isNodejs()).to.equal(true);
    });
    it("Should pretty-print json properly", () => {
        const someObject = { number: 1, string: "2", anonymousFunction: () => { }, namedFunction: function bla() { } };
        someObject.circular = someObject;
        expect(safeStringify(someObject)).to.equal(`{
  "number": 1,
  "string": "2",
  "anonymousFunction": "anonymousFunction()",
  "namedFunction": "bla()",
  "circular": "__circular__"
}`);
    });
});
//# sourceMappingURL=index.test.js.map