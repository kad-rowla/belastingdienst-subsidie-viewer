import { TriplyDbJsError } from "@triply/triplydb/utils/Error.js";
import { Etl, Iri } from "../generic/index.js";
export default async function (): Promise<Etl> {
  const app = new Etl({
    prefixes: { bla: Iri("https://sdf") },
    defaultGraph: "https://default.com/",
  });

  app.use(() => {
    throw new TriplyDbJsError("Not found").setStatusCode(404);
  });

  return app;
}
