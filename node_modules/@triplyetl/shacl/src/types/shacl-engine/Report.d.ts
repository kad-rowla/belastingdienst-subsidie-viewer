declare module 'shacl-engine/lib/Report.js' {
  import { type DefaultFactory, type Store } from '@triplydb/data-factory'
  // import type Result from 'shacl-engine/lib/Result.js'

  interface Options {
    factory: DefaultFactory
    debug?: boolean
    details?: boolean
    trace?: boolean
    coverage?: boolean
  }

  interface ReportArguments {
    details?: boolean
    factory: DefaultFactory
    options?: Options
    results: any[]
  }

  declare class Report {
    constructor (args: ReportArguments)
    details: boolean
    factory: DefaultFactory
    options: Options

    get conforms (): boolean
    dataset: Store
    results: any[]
  }
  export default Report
}
