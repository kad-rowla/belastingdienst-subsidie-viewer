import { Store, parse } from '@triplydb/data-factory'
import { readFileSync } from 'fs'
import { join } from 'path'
export default (file: string, ...path: string[]): Store =>
  new Store(parse(readFileSync(join(process.cwd(), ...path, file), 'utf-8')))
