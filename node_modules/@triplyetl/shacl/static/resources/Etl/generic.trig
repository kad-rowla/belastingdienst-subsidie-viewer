prefix owl: <http://www.w3.org/2002/07/owl#>
prefix rdf: <http://www.w3.org/1999/02/22-rdf-syntax-ns#>
prefix rdfs: <http://www.w3.org/2000/01/rdf-schema#>
prefix rel: <http://id.loc.gov/vocabulary/relators/>
prefix rico: <https://www.ica.org/standards/RiC/ontology#>
prefix sdo: <https://schema.org/>
prefix sh: <http://www.w3.org/ns/shacl#>
prefix skos: <http://www.w3.org/2004/02/skos/core#>
prefix xsd: <http://www.w3.org/2001/XMLSchema#>

prefix graph: <https://collectiedata.hetnieuweinstituut.nl/graph/>
prefix lst: <https://collectiedata.hetnieuweinstituut.nl/model/lst/>
prefix shp: <https://collectiedata.hetnieuweinstituut.nl/model/shp/>

graph:model {

 #shp:Resource
 #  a sh:NodeShape;
 #  sh:property
 #    shp:Resource_altLabel,
 #    shp:Resource_hiddenLabel,
 #    shp:Resource_prefLabel,
 #     shp:Resource_sameAs,
 #    shp:Resource_scopeNote,
 #    shp:Resource_seeAlso;
 #  sh:targetClass skos:Concept.

  shp:Resource_altLabel
    a sh:PropertyShape;
    sh:or lst:langString_or_string;
    sh:path skos:altLabel.

  shp:Resource_hiddenLabel
    a sh:PropertyShape;
    sh:or lst:langString_or_string;
    sh:path skos:hiddenLabel.

  shp:Resource_prefLabel
    a sh:PropertyShape;
    sh:or lst:langString_or_string;
    sh:maxCount 1;
    sh:minCount 1;
    sh:path skos:prefLabel.

  shp:Resource_sameAs
    a sh:PropertyShape;
    sh:and ( shp:iri );
    sh:path owl:sameAs.

  shp:Resource_scopeNote
    a sh:PropertyShape;
    sh:or lst:langString_or_string;
    sh:maxCount 1;
    sh:path skos:scopeNote.

  shp:Resource_seeAlso
    a sh:PropertyShape;
    sh:datatype xsd:anyURI;
    sh:path rdfs:seeAlso.



  shp:Resource_abr
    a sh:PropertyShape;
    sh:and ( shp:iri );
    sh:path rel:abr.

  shp:Resource_acp
    a sh:PropertyShape;
    sh:and ( shp:iri );
    sh:path rel:acp.

  shp:Resource_act
    a sh:PropertyShape;
    sh:and ( shp:iri );
    sh:path rel:act.

  shp:Resource_adi
    a sh:PropertyShape;
    sh:and ( shp:iri );
    sh:path rel:adi.

  shp:Resource_adp
    a sh:PropertyShape;
    sh:and ( shp:iri );
    sh:path rel:adp.

  shp:Resource_aft
    a sh:PropertyShape;
    sh:and ( shp:iri );
    sh:path rel:aft.

  shp:Resource_anl
    a sh:PropertyShape;
    sh:and ( shp:iri );
    sh:path rel:anl.

  shp:Resource_anm
    a sh:PropertyShape;
    sh:and ( shp:iri );
    sh:path rel:anm.

  shp:Resource_ann
    a sh:PropertyShape;
    sh:and ( shp:iri );
    sh:path rel:ann.

  shp:Resource_ant
    a sh:PropertyShape;
    sh:and ( shp:iri );
    sh:path rel:ant.

  shp:Resource_ape
    a sh:PropertyShape;
    sh:and ( shp:iri );
    sh:path rel:ape.

  shp:Resource_apl
    a sh:PropertyShape;
    sh:and ( shp:iri );
    sh:path rel:apl.

  shp:Resource_app
    a sh:PropertyShape;
    sh:and ( shp:iri );
    sh:path rel:app.

  shp:Resource_aqt
    a sh:PropertyShape;
    sh:and ( shp:iri );
    sh:path rel:aqt.

  shp:Resource_arc
    a sh:PropertyShape;
    sh:and ( shp:iri );
    sh:path rel:arc.

  shp:Resource_ard
    a sh:PropertyShape;
    sh:and ( shp:iri );
    sh:path rel:ard.

  shp:Resource_arr
    a sh:PropertyShape;
    sh:and ( shp:iri );
    sh:path rel:arr.

  shp:Resource_art
    a sh:PropertyShape;
    sh:and ( shp:iri );
    sh:path rel:art.

  shp:Resource_asg
    a sh:PropertyShape;
    sh:and ( shp:iri );
    sh:path rel:asg.

  shp:Resource_asn
    a sh:PropertyShape;
    sh:and ( shp:iri );
    sh:path rel:asn.

  shp:Resource_ato
    a sh:PropertyShape;
    sh:and ( shp:iri );
    sh:path rel:ato.

  shp:Resource_att
    a sh:PropertyShape;
    sh:and ( shp:iri );
    sh:path rel:att.

  shp:Resource_auc
    a sh:PropertyShape;
    sh:and ( shp:iri );
    sh:path rel:auc.

  shp:Resource_aud
    a sh:PropertyShape;
    sh:and ( shp:iri );
    sh:path rel:aud.

  shp:Resource_aui
    a sh:PropertyShape;
    sh:and ( shp:iri );
    sh:path rel:aui.

  shp:Resource_aus
    a sh:PropertyShape;
    sh:and ( shp:iri );
    sh:path rel:aus.

  shp:Resource_aut
    a sh:PropertyShape;
    sh:and ( shp:iri );
    sh:path rel:aut.

  shp:Resource_bdd
    a sh:PropertyShape;
    sh:and ( shp:iri );
    sh:path rel:bdd.

  shp:Resource_bjd
    a sh:PropertyShape;
    sh:and ( shp:iri );
    sh:path rel:bjd.

  shp:Resource_bkd
    a sh:PropertyShape;
    sh:and ( shp:iri );
    sh:path rel:bkd.

  shp:Resource_bkp
    a sh:PropertyShape;
    sh:and ( shp:iri );
    sh:path rel:bkp.

  shp:Resource_blw
    a sh:PropertyShape;
    sh:and ( shp:iri );
    sh:path rel:blw.

  shp:Resource_bnd
    a sh:PropertyShape;
    sh:and ( shp:iri );
    sh:path rel:bnd.

  shp:Resource_bpd
    a sh:PropertyShape;
    sh:and ( shp:iri );
    sh:path rel:bpd.

  shp:Resource_brd
    a sh:PropertyShape;
    sh:and ( shp:iri );
    sh:path rel:brd.

  shp:Resource_brl
    a sh:PropertyShape;
    sh:and ( shp:iri );
    sh:path rel:brl.

  shp:Resource_bsl
    a sh:PropertyShape;
    sh:and ( shp:iri );
    sh:path rel:bsl.

  shp:Resource_cas
    a sh:PropertyShape;
    sh:and ( shp:iri );
    sh:path rel:cas.

  shp:Resource_ccp
    a sh:PropertyShape;
    sh:and ( shp:iri );
    sh:path rel:ccp.

  shp:Resource_chr
    a sh:PropertyShape;
    sh:and ( shp:iri );
    sh:path rel:chr.

  shp:Resource_cli
    a sh:PropertyShape;
    sh:and ( shp:iri );
    sh:path rel:cli.

  shp:Resource_cll
    a sh:PropertyShape;
    sh:and ( shp:iri );
    sh:path rel:cll.

  shp:Resource_clr
    a sh:PropertyShape;
    sh:and ( shp:iri );
    sh:path rel:clr.

  shp:Resource_clt
    a sh:PropertyShape;
    sh:and ( shp:iri );
    sh:path rel:clt.

  shp:Resource_cmm
    a sh:PropertyShape;
    sh:and ( shp:iri );
    sh:path rel:cmm.

  shp:Resource_cmp
    a sh:PropertyShape;
    sh:and ( shp:iri );
    sh:path rel:cmp.

  shp:Resource_cmt
    a sh:PropertyShape;
    sh:and ( shp:iri );
    sh:path rel:cmt.

  shp:Resource_cnd
    a sh:PropertyShape;
    sh:and ( shp:iri );
    sh:path rel:cnd.

  shp:Resource_cng
    a sh:PropertyShape;
    sh:and ( shp:iri );
    sh:path rel:cng.

  shp:Resource_cns
    a sh:PropertyShape;
    sh:and ( shp:iri );
    sh:path rel:cns.

  shp:Resource_coe
    a sh:PropertyShape;
    sh:and ( shp:iri );
    sh:path rel:coe.

  shp:Resource_col
    a sh:PropertyShape;
    sh:and ( shp:iri );
    sh:path rel:col.

  shp:Resource_com
    a sh:PropertyShape;
    sh:and ( shp:iri );
    sh:path rel:com.

  shp:Resource_con
    a sh:PropertyShape;
    sh:and ( shp:iri );
    sh:path rel:con.

  shp:Resource_cor
    a sh:PropertyShape;
    sh:and ( shp:iri );
    sh:path rel:cor.

  shp:Resource_cos
    a sh:PropertyShape;
    sh:and ( shp:iri );
    sh:path rel:cos.

  shp:Resource_cot
    a sh:PropertyShape;
    sh:and ( shp:iri );
    sh:path rel:cot.

  shp:Resource_cou
    a sh:PropertyShape;
    sh:and ( shp:iri );
    sh:path rel:cou.

  shp:Resource_cov
    a sh:PropertyShape;
    sh:and ( shp:iri );
    sh:path rel:cov.

  shp:Resource_cpc
    a sh:PropertyShape;
    sh:and ( shp:iri );
    sh:path rel:cpc.

  shp:Resource_cpe
    a sh:PropertyShape;
    sh:and ( shp:iri );
    sh:path rel:cpe.

  shp:Resource_cph
    a sh:PropertyShape;
    sh:and ( shp:iri );
    sh:path rel:cph.

  shp:Resource_cpl
    a sh:PropertyShape;
    sh:and ( shp:iri );
    sh:path rel:cpl.

  shp:Resource_cpt
    a sh:PropertyShape;
    sh:and ( shp:iri );
    sh:path rel:cpt.

  shp:Resource_cre
    a sh:PropertyShape;
    sh:and ( shp:iri );
    sh:path rel:cre.

  shp:Resource_crp
    a sh:PropertyShape;
    sh:and ( shp:iri );
    sh:path rel:crp.

  shp:Resource_crr
    a sh:PropertyShape;
    sh:and ( shp:iri );
    sh:path rel:crr.

  shp:Resource_crt
    a sh:PropertyShape;
    sh:and ( shp:iri );
    sh:path rel:crt.

  shp:Resource_csl
    a sh:PropertyShape;
    sh:and ( shp:iri );
    sh:path rel:csl.

  shp:Resource_csp
    a sh:PropertyShape;
    sh:and ( shp:iri );
    sh:path rel:csp.

  shp:Resource_cst
    a sh:PropertyShape;
    sh:and ( shp:iri );
    sh:path rel:cst.

  shp:Resource_ctb
    a sh:PropertyShape;
    sh:and ( shp:iri );
    sh:path rel:ctb.

  shp:Resource_cte
    a sh:PropertyShape;
    sh:and ( shp:iri );
    sh:path rel:cte.

  shp:Resource_ctg
    a sh:PropertyShape;
    sh:and ( shp:iri );
    sh:path rel:ctg.

  shp:Resource_ctr
    a sh:PropertyShape;
    sh:and ( shp:iri );
    sh:path rel:ctr.

  shp:Resource_cts
    a sh:PropertyShape;
    sh:and ( shp:iri );
    sh:path rel:cts.

  shp:Resource_ctt
    a sh:PropertyShape;
    sh:and ( shp:iri );
    sh:path rel:ctt.

  shp:Resource_cur
    a sh:PropertyShape;
    sh:and ( shp:iri );
    sh:path rel:cur.

  shp:Resource_cwt
    a sh:PropertyShape;
    sh:and ( shp:iri );
    sh:path rel:cwt.

  shp:Resource_dbp
    a sh:PropertyShape;
    sh:and ( shp:iri );
    sh:path rel:dbp.

  shp:Resource_dfd
    a sh:PropertyShape;
    sh:and ( shp:iri );
    sh:path rel:dfd.

  shp:Resource_dfe
    a sh:PropertyShape;
    sh:and ( shp:iri );
    sh:path rel:dfe.

  shp:Resource_dft
    a sh:PropertyShape;
    sh:and ( shp:iri );
    sh:path rel:dft.

  shp:Resource_dgg
    a sh:PropertyShape;
    sh:and ( shp:iri );
    sh:path rel:dgg.

  shp:Resource_dgs
    a sh:PropertyShape;
    sh:and ( shp:iri );
    sh:path rel:dgs.

  shp:Resource_dis
    a sh:PropertyShape;
    sh:and ( shp:iri );
    sh:path rel:dis.

  shp:Resource_dln
    a sh:PropertyShape;
    sh:and ( shp:iri );
    sh:path rel:dln.

  shp:Resource_dnc
    a sh:PropertyShape;
    sh:and ( shp:iri );
    sh:path rel:dnc.

  shp:Resource_dnr
    a sh:PropertyShape;
    sh:and ( shp:iri );
    sh:path rel:dnr.

  shp:Resource_dpc
    a sh:PropertyShape;
    sh:and ( shp:iri );
    sh:path rel:dpc.

  shp:Resource_dpt
    a sh:PropertyShape;
    sh:and ( shp:iri );
    sh:path rel:dpt.

  shp:Resource_drm
    a sh:PropertyShape;
    sh:and ( shp:iri );
    sh:path rel:drm.

  shp:Resource_drt
    a sh:PropertyShape;
    sh:and ( shp:iri );
    sh:path rel:drt.

  shp:Resource_dsr
    a sh:PropertyShape;
    sh:and ( shp:iri );
    sh:path rel:dsr.

  shp:Resource_dst
    a sh:PropertyShape;
    sh:and ( shp:iri );
    sh:path rel:dst.

  shp:Resource_dtc
    a sh:PropertyShape;
    sh:and ( shp:iri );
    sh:path rel:dtc.

  shp:Resource_dte
    a sh:PropertyShape;
    sh:and ( shp:iri );
    sh:path rel:dte.

  shp:Resource_dtm
    a sh:PropertyShape;
    sh:and ( shp:iri );
    sh:path rel:dtm.

  shp:Resource_dto
    a sh:PropertyShape;
    sh:and ( shp:iri );
    sh:path rel:dto.

  shp:Resource_dub
    a sh:PropertyShape;
    sh:and ( shp:iri );
    sh:path rel:dub.

  shp:Resource_edc
    a sh:PropertyShape;
    sh:and ( shp:iri );
    sh:path rel:edc.

  shp:Resource_edm
    a sh:PropertyShape;
    sh:and ( shp:iri );
    sh:path rel:edm.

  shp:Resource_edt
    a sh:PropertyShape;
    sh:and ( shp:iri );
    sh:path rel:edt.

  shp:Resource_egr
    a sh:PropertyShape;
    sh:and ( shp:iri );
    sh:path rel:egr.

  shp:Resource_elg
    a sh:PropertyShape;
    sh:and ( shp:iri );
    sh:path rel:elg.

  shp:Resource_elt
    a sh:PropertyShape;
    sh:and ( shp:iri );
    sh:path rel:elt.

  shp:Resource_eng
    a sh:PropertyShape;
    sh:and ( shp:iri );
    sh:path rel:eng.

  shp:Resource_enj
    a sh:PropertyShape;
    sh:and ( shp:iri );
    sh:path rel:enj.

  shp:Resource_etr
    a sh:PropertyShape;
    sh:and ( shp:iri );
    sh:path rel:etr.

  shp:Resource_evp
    a sh:PropertyShape;
    sh:and ( shp:iri );
    sh:path rel:evp.

  shp:Resource_exp
    a sh:PropertyShape;
    sh:and ( shp:iri );
    sh:path rel:exp.

  shp:Resource_fac
    a sh:PropertyShape;
    sh:and ( shp:iri );
    sh:path rel:fac.

  shp:Resource_fds
    a sh:PropertyShape;
    sh:and ( shp:iri );
    sh:path rel:fds.

  shp:Resource_fld
    a sh:PropertyShape;
    sh:and ( shp:iri );
    sh:path rel:fld.

  shp:Resource_flm
    a sh:PropertyShape;
    sh:and ( shp:iri );
    sh:path rel:flm.

  shp:Resource_fmd
    a sh:PropertyShape;
    sh:and ( shp:iri );
    sh:path rel:fmd.

  shp:Resource_fmk
    a sh:PropertyShape;
    sh:and ( shp:iri );
    sh:path rel:fmk.

  shp:Resource_fmo
    a sh:PropertyShape;
    sh:and ( shp:iri );
    sh:path rel:fmo.

  shp:Resource_fmp
    a sh:PropertyShape;
    sh:and ( shp:iri );
    sh:path rel:fmp.

  shp:Resource_fnd
    a sh:PropertyShape;
    sh:and ( shp:iri );
    sh:path rel:fnd.

  shp:Resource_fpy
    a sh:PropertyShape;
    sh:and ( shp:iri );
    sh:path rel:fpy.

  shp:Resource_frg
    a sh:PropertyShape;
    sh:and ( shp:iri );
    sh:path rel:frg.

  shp:Resource_gis
    a sh:PropertyShape;
    sh:and ( shp:iri );
    sh:path rel:gis.

  shp:Resource_his
    a sh:PropertyShape;
    sh:and ( shp:iri );
    sh:path rel:his.

  shp:Resource_hnr
    a sh:PropertyShape;
    sh:and ( shp:iri );
    sh:path rel:hnr.

  shp:Resource_hst
    a sh:PropertyShape;
    sh:and ( shp:iri );
    sh:path rel:hst.

  shp:Resource_ill
    a sh:PropertyShape;
    sh:and ( shp:iri );
    sh:path rel:ill.

  shp:Resource_ilu
    a sh:PropertyShape;
    sh:and ( shp:iri );
    sh:path rel:ilu.

  shp:Resource_ins
    a sh:PropertyShape;
    sh:and ( shp:iri );
    sh:path rel:ins.

  shp:Resource_inv
    a sh:PropertyShape;
    sh:and ( shp:iri );
    sh:path rel:inv.

  shp:Resource_isb
    a sh:PropertyShape;
    sh:and ( shp:iri );
    sh:path rel:isb.

  shp:Resource_itr
    a sh:PropertyShape;
    sh:and ( shp:iri );
    sh:path rel:itr.

  shp:Resource_ive
    a sh:PropertyShape;
    sh:and ( shp:iri );
    sh:path rel:ive.

  shp:Resource_ivr
    a sh:PropertyShape;
    sh:and ( shp:iri );
    sh:path rel:ivr.

  shp:Resource_jud
    a sh:PropertyShape;
    sh:and ( shp:iri );
    sh:path rel:jud.

  shp:Resource_jug
    a sh:PropertyShape;
    sh:and ( shp:iri );
    sh:path rel:jug.

  shp:Resource_lbr
    a sh:PropertyShape;
    sh:and ( shp:iri );
    sh:path rel:lbr.

  shp:Resource_lbt
    a sh:PropertyShape;
    sh:and ( shp:iri );
    sh:path rel:lbt.

  shp:Resource_ldr
    a sh:PropertyShape;
    sh:and ( shp:iri );
    sh:path rel:ldr.

  shp:Resource_led
    a sh:PropertyShape;
    sh:and ( shp:iri );
    sh:path rel:led.

  shp:Resource_lee
    a sh:PropertyShape;
    sh:and ( shp:iri );
    sh:path rel:lee.

  shp:Resource_lel
    a sh:PropertyShape;
    sh:and ( shp:iri );
    sh:path rel:lel.

  shp:Resource_len
    a sh:PropertyShape;
    sh:and ( shp:iri );
    sh:path rel:len.

  shp:Resource_let
    a sh:PropertyShape;
    sh:and ( shp:iri );
    sh:path rel:let.

  shp:Resource_lgd
    a sh:PropertyShape;
    sh:and ( shp:iri );
    sh:path rel:lgd.

  shp:Resource_lie
    a sh:PropertyShape;
    sh:and ( shp:iri );
    sh:path rel:lie.

  shp:Resource_lil
    a sh:PropertyShape;
    sh:and ( shp:iri );
    sh:path rel:lil.

  shp:Resource_lit
    a sh:PropertyShape;
    sh:and ( shp:iri );
    sh:path rel:lit.

  shp:Resource_lsa
    a sh:PropertyShape;
    sh:and ( shp:iri );
    sh:path rel:lsa.

  shp:Resource_lse
    a sh:PropertyShape;
    sh:and ( shp:iri );
    sh:path rel:lse.

  shp:Resource_lso
    a sh:PropertyShape;
    sh:and ( shp:iri );
    sh:path rel:lso.

  shp:Resource_ltg
    a sh:PropertyShape;
    sh:and ( shp:iri );
    sh:path rel:ltg.

  shp:Resource_lyr
    a sh:PropertyShape;
    sh:and ( shp:iri );
    sh:path rel:lyr.

  shp:Resource_mcp
    a sh:PropertyShape;
    sh:and ( shp:iri );
    sh:path rel:mcp.

  shp:Resource_mdc
    a sh:PropertyShape;
    sh:and ( shp:iri );
    sh:path rel:mdc.

  shp:Resource_med
    a sh:PropertyShape;
    sh:and ( shp:iri );
    sh:path rel:med.

  shp:Resource_mfp
    a sh:PropertyShape;
    sh:and ( shp:iri );
    sh:path rel:mfp.

  shp:Resource_mfr
    a sh:PropertyShape;
    sh:and ( shp:iri );
    sh:path rel:mfr.

  shp:Resource_mod
    a sh:PropertyShape;
    sh:and ( shp:iri );
    sh:path rel:mod.

  shp:Resource_mon
    a sh:PropertyShape;
    sh:and ( shp:iri );
    sh:path rel:mon.

  shp:Resource_mrb
    a sh:PropertyShape;
    sh:and ( shp:iri );
    sh:path rel:mrb.

  shp:Resource_mrk
    a sh:PropertyShape;
    sh:and ( shp:iri );
    sh:path rel:mrk.

  shp:Resource_msd
    a sh:PropertyShape;
    sh:and ( shp:iri );
    sh:path rel:msd.

  shp:Resource_mte
    a sh:PropertyShape;
    sh:and ( shp:iri );
    sh:path rel:mte.

  shp:Resource_mtk
    a sh:PropertyShape;
    sh:and ( shp:iri );
    sh:path rel:mtk.

  shp:Resource_mus
    a sh:PropertyShape;
    sh:and ( shp:iri );
    sh:path rel:mus.

  shp:Resource_nrt
    a sh:PropertyShape;
    sh:and ( shp:iri );
    sh:path rel:nrt.

  shp:Resource_opn
    a sh:PropertyShape;
    sh:and ( shp:iri );
    sh:path rel:opn.

  shp:Resource_org
    a sh:PropertyShape;
    sh:and ( shp:iri );
    sh:path rel:org.

  shp:Resource_orm
    a sh:PropertyShape;
    sh:and ( shp:iri );
    sh:path rel:orm.

  shp:Resource_osp
    a sh:PropertyShape;
    sh:and ( shp:iri );
    sh:path rel:osp.

  shp:Resource_oth
    a sh:PropertyShape;
    sh:and ( shp:iri );
    sh:path rel:oth.

  shp:Resource_own
    a sh:PropertyShape;
    sh:and ( shp:iri );
    sh:path rel:own.

  shp:Resource_pan
    a sh:PropertyShape;
    sh:and ( shp:iri );
    sh:path rel:pan.

  shp:Resource_pat
    a sh:PropertyShape;
    sh:and ( shp:iri );
    sh:path rel:pat.

  shp:Resource_pbd
    a sh:PropertyShape;
    sh:and ( shp:iri );
    sh:path rel:pbd.

  shp:Resource_pbl
    a sh:PropertyShape;
    sh:and ( shp:iri );
    sh:path rel:pbl.

  shp:Resource_pdr
    a sh:PropertyShape;
    sh:and ( shp:iri );
    sh:path rel:pdr.

  shp:Resource_pfr
    a sh:PropertyShape;
    sh:and ( shp:iri );
    sh:path rel:pfr.

  shp:Resource_pht
    a sh:PropertyShape;
    sh:and ( shp:iri );
    sh:path rel:pht.

  shp:Resource_plt
    a sh:PropertyShape;
    sh:and ( shp:iri );
    sh:path rel:plt.

  shp:Resource_pma
    a sh:PropertyShape;
    sh:and ( shp:iri );
    sh:path rel:pma.

  shp:Resource_pmn
    a sh:PropertyShape;
    sh:and ( shp:iri );
    sh:path rel:pmn.

  shp:Resource_pop
    a sh:PropertyShape;
    sh:and ( shp:iri );
    sh:path rel:pop.

  shp:Resource_ppm
    a sh:PropertyShape;
    sh:and ( shp:iri );
    sh:path rel:ppm.

  shp:Resource_ppt
    a sh:PropertyShape;
    sh:and ( shp:iri );
    sh:path rel:ppt.

  shp:Resource_pra
    a sh:PropertyShape;
    sh:and ( shp:iri );
    sh:path rel:pra.

  shp:Resource_prc
    a sh:PropertyShape;
    sh:and ( shp:iri );
    sh:path rel:prc.

  shp:Resource_prd
    a sh:PropertyShape;
    sh:and ( shp:iri );
    sh:path rel:prd.

  shp:Resource_pre
    a sh:PropertyShape;
    sh:and ( shp:iri );
    sh:path rel:pre.

  shp:Resource_prf
    a sh:PropertyShape;
    sh:and ( shp:iri );
    sh:path rel:prf.

  shp:Resource_prg
    a sh:PropertyShape;
    sh:and ( shp:iri );
    sh:path rel:prg.

  shp:Resource_prm
    a sh:PropertyShape;
    sh:and ( shp:iri );
    sh:path rel:prm.

  shp:Resource_prn
    a sh:PropertyShape;
    sh:and ( shp:iri );
    sh:path rel:prn.

  shp:Resource_pro
    a sh:PropertyShape;
    sh:and ( shp:iri );
    sh:path rel:pro.

  shp:Resource_prp
    a sh:PropertyShape;
    sh:and ( shp:iri );
    sh:path rel:prp.

  shp:Resource_prs
    a sh:PropertyShape;
    sh:and ( shp:iri );
    sh:path rel:prs.

  shp:Resource_prt
    a sh:PropertyShape;
    sh:and ( shp:iri );
    sh:path rel:prt.

  shp:Resource_prv
    a sh:PropertyShape;
    sh:and ( shp:iri );
    sh:path rel:prv.

  shp:Resource_pta
    a sh:PropertyShape;
    sh:and ( shp:iri );
    sh:path rel:pta.

  shp:Resource_pte
    a sh:PropertyShape;
    sh:and ( shp:iri );
    sh:path rel:pte.

  shp:Resource_ptf
    a sh:PropertyShape;
    sh:and ( shp:iri );
    sh:path rel:ptf.

  shp:Resource_pth
    a sh:PropertyShape;
    sh:and ( shp:iri );
    sh:path rel:pth.

  shp:Resource_ptt
    a sh:PropertyShape;
    sh:and ( shp:iri );
    sh:path rel:ptt.

  shp:Resource_pup
    a sh:PropertyShape;
    sh:and ( shp:iri );
    sh:path rel:pup.

  shp:Resource_rbr
    a sh:PropertyShape;
    sh:and ( shp:iri );
    sh:path rel:rbr.

  shp:Resource_rcd
    a sh:PropertyShape;
    sh:and ( shp:iri );
    sh:path rel:rcd.

  shp:Resource_rce
    a sh:PropertyShape;
    sh:and ( shp:iri );
    sh:path rel:rce.

  shp:Resource_rcp
    a sh:PropertyShape;
    sh:and ( shp:iri );
    sh:path rel:rcp.

  shp:Resource_rdd
    a sh:PropertyShape;
    sh:and ( shp:iri );
    sh:path rel:rdd.

  shp:Resource_red
    a sh:PropertyShape;
    sh:and ( shp:iri );
    sh:path rel:red.

  shp:Resource_ren
    a sh:PropertyShape;
    sh:and ( shp:iri );
    sh:path rel:ren.

  shp:Resource_res
    a sh:PropertyShape;
    sh:and ( shp:iri );
    sh:path rel:res.

  shp:Resource_rev
    a sh:PropertyShape;
    sh:and ( shp:iri );
    sh:path rel:rev.

  shp:Resource_role
    a sh:PropertyShape;
    sh:and ( shp:iri );
    sh:path rel:role.

  shp:Resource_rpc
    a sh:PropertyShape;
    sh:and ( shp:iri );
    sh:path rel:rpc.

  shp:Resource_rps
    a sh:PropertyShape;
    sh:and ( shp:iri );
    sh:path rel:rps.

  shp:Resource_rpt
    a sh:PropertyShape;
    sh:and ( shp:iri );
    sh:path rel:rpt.

  shp:Resource_rpy
    a sh:PropertyShape;
    sh:and ( shp:iri );
    sh:path rel:rpy.

  shp:Resource_rse
    a sh:PropertyShape;
    sh:and ( shp:iri );
    sh:path rel:rse.

  shp:Resource_rsg
    a sh:PropertyShape;
    sh:and ( shp:iri );
    sh:path rel:rsg.

  shp:Resource_rsp
    a sh:PropertyShape;
    sh:and ( shp:iri );
    sh:path rel:rsp.

  shp:Resource_rsr
    a sh:PropertyShape;
    sh:and ( shp:iri );
    sh:path rel:rsr.

  shp:Resource_rst
    a sh:PropertyShape;
    sh:and ( shp:iri );
    sh:path rel:rst.

  shp:Resource_rth
    a sh:PropertyShape;
    sh:and ( shp:iri );
    sh:path rel:rth.

  shp:Resource_rtm
    a sh:PropertyShape;
    sh:and ( shp:iri );
    sh:path rel:rtm.

  shp:Resource_sad
    a sh:PropertyShape;
    sh:and ( shp:iri );
    sh:path rel:sad.

  shp:Resource_sce
    a sh:PropertyShape;
    sh:and ( shp:iri );
    sh:path rel:sce.

  shp:Resource_scl
    a sh:PropertyShape;
    sh:and ( shp:iri );
    sh:path rel:scl.

  shp:Resource_scr
    a sh:PropertyShape;
    sh:and ( shp:iri );
    sh:path rel:scr.

  shp:Resource_sds
    a sh:PropertyShape;
    sh:and ( shp:iri );
    sh:path rel:sds.

  shp:Resource_sec
    a sh:PropertyShape;
    sh:and ( shp:iri );
    sh:path rel:sec.

  shp:Resource_sgd
    a sh:PropertyShape;
    sh:and ( shp:iri );
    sh:path rel:sgd.

  shp:Resource_sgn
    a sh:PropertyShape;
    sh:and ( shp:iri );
    sh:path rel:sgn.

  shp:Resource_sht
    a sh:PropertyShape;
    sh:and ( shp:iri );
    sh:path rel:sht.

  shp:Resource_sll
    a sh:PropertyShape;
    sh:and ( shp:iri );
    sh:path rel:sll.

  shp:Resource_sng
    a sh:PropertyShape;
    sh:and ( shp:iri );
    sh:path rel:sng.

  shp:Resource_spk
    a sh:PropertyShape;
    sh:and ( shp:iri );
    sh:path rel:spk.

  shp:Resource_spn
    a sh:PropertyShape;
    sh:and ( shp:iri );
    sh:path rel:spn.

  shp:Resource_spy
    a sh:PropertyShape;
    sh:and ( shp:iri );
    sh:path rel:spy.

  shp:Resource_srv
    a sh:PropertyShape;
    sh:and ( shp:iri );
    sh:path rel:srv.

  shp:Resource_std
    a sh:PropertyShape;
    sh:and ( shp:iri );
    sh:path rel:std.

  shp:Resource_stg
    a sh:PropertyShape;
    sh:and ( shp:iri );
    sh:path rel:stg.

  shp:Resource_stl
    a sh:PropertyShape;
    sh:and ( shp:iri );
    sh:path rel:stl.

  shp:Resource_stm
    a sh:PropertyShape;
    sh:and ( shp:iri );
    sh:path rel:stm.

  shp:Resource_stn
    a sh:PropertyShape;
    sh:and ( shp:iri );
    sh:path rel:stn.

  shp:Resource_str
    a sh:PropertyShape;
    sh:and ( shp:iri );
    sh:path rel:str.

  shp:Resource_tcd
    a sh:PropertyShape;
    sh:and ( shp:iri );
    sh:path rel:tcd.

  shp:Resource_tch
    a sh:PropertyShape;
    sh:and ( shp:iri );
    sh:path rel:tch.

  shp:Resource_ths
    a sh:PropertyShape;
    sh:and ( shp:iri );
    sh:path rel:ths.

  shp:Resource_tld
    a sh:PropertyShape;
    sh:and ( shp:iri );
    sh:path rel:tld.

  shp:Resource_tlp
    a sh:PropertyShape;
    sh:and ( shp:iri );
    sh:path rel:tlp.

  shp:Resource_trc
    a sh:PropertyShape;
    sh:and ( shp:iri );
    sh:path rel:trc.

  shp:Resource_trl
    a sh:PropertyShape;
    sh:and ( shp:iri );
    sh:path rel:trl.

  shp:Resource_tyd
    a sh:PropertyShape;
    sh:and ( shp:iri );
    sh:path rel:tyd.

  shp:Resource_tyg
    a sh:PropertyShape;
    sh:and ( shp:iri );
    sh:path rel:tyg.

  shp:Resource_uvp
    a sh:PropertyShape;
    sh:and ( shp:iri );
    sh:path rel:uvp.

  shp:Resource_vac
    a sh:PropertyShape;
    sh:and ( shp:iri );
    sh:path rel:vac.

  shp:Resource_vdg
    a sh:PropertyShape;
    sh:and ( shp:iri );
    sh:path rel:vdg.

  shp:Resource_wac
    a sh:PropertyShape;
    sh:and ( shp:iri );
    sh:path rel:wac.

  shp:Resource_wal
    a sh:PropertyShape;
    sh:and ( shp:iri );
    sh:path rel:wal.

  shp:Resource_wam
    a sh:PropertyShape;
    sh:and ( shp:iri );
    sh:path rel:wam.

  shp:Resource_wat
    a sh:PropertyShape;
    sh:and ( shp:iri );
    sh:path rel:wat.

  shp:Resource_wdc
    a sh:PropertyShape;
    sh:and ( shp:iri );
    sh:path rel:wdc.

  shp:Resource_wde
    a sh:PropertyShape;
    sh:and ( shp:iri );
    sh:path rel:wde.

  shp:Resource_win
    a sh:PropertyShape;
    sh:and ( shp:iri );
    sh:path rel:win.

  shp:Resource_wit
    a sh:PropertyShape;
    sh:and ( shp:iri );
    sh:path rel:wit.

  shp:Resource_wpr
    a sh:PropertyShape;
    sh:and ( shp:iri );
    sh:path rel:wpr.

  shp:Resource_wst
    a sh:PropertyShape;
    sh:and ( shp:iri );
    sh:path rel:wst.



 #shp:Thing
 #  a sh:NodeShape;
 #  sh:property
 #    shp:Thing_additionalType,
 #    shp:Thing_description,
 #    shp:Thing_url;
 #  sh:targetClass sdo:Thing.

  shp:Thing_additionalType
    a sh:PropertyShape;
    sh:and ( shp:iri );
    sh:path sdo:additionalType.

  shp:Thing_description
    a sh:PropertyShape;
    sh:or lst:langString_or_string;
    sh:path sdo:description.

  shp:Thing_url
    a sh:PropertyShape;
    sh:datatype xsd:anyURI;
    sh:path sdo:url.



  lst:langString_or_string
    a rdf:list;
    rdf:first shp:langString;
    rdf:rest lst:string.
  lst:string
    a rdf:List;
    rdf:first shp:string;
    rdf:rest rdf:nil.

  shp:iri
    a sh:PropertyShape;
    sh:nodekind sh:IRI.

  shp:langString
    a sh:PropertyShape;
    sh:datatype rdf:langString.

  shp:literal
    a sh:PropertyShape;
    sh:minLength 1;
    sh:nodeKind sh:Literal.

  shp:string
    a sh:PropertyShape;
    sh:datatype xsd:string.

}
