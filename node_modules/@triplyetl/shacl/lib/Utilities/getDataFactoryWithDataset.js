import { Store, getFactory } from '@triplydb/data-factory';
const getDatasetFactory = (quads) => new Store(quads !== null && quads !== void 0 ? quads : [], { factory: getFactory() });
export default function getDataFactoryWithDataset(factory) {
    return Object.assign(Object.assign({}, factory !== null && factory !== void 0 ? factory : getFactory()), { dataset: getDatasetFactory });
}
//# sourceMappingURL=getDataFactoryWithDataset.js.map