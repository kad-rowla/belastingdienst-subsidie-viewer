import { type DefaultFactory, type Store } from '@triplydb/data-factory';
import { type SparqlEngine } from '@triplydb/engine/SparqlEngine.js';
import { Iri } from '@triplyetl/vocabularies';
/**
 * Implements 5.2.1 Prefix Declarations for SPARQL Queries
 *
 * @see https://w3c.github.io/data-shapes/shacl/#sparql-prefixes
 */
declare function prefixDeclarations(datasetOrEngine: Store, factory?: DefaultFactory): Promise<Record<string, Iri>>;
declare function prefixDeclarations(datasetOrEngine: SparqlEngine, factory?: DefaultFactory): Promise<Record<string, Iri>>;
export default prefixDeclarations;
//# sourceMappingURL=prefixDeclarations.d.ts.map