import { Store, parse } from '@triplydb/data-factory';
import { readFileSync } from 'fs';
import { join } from 'path';
export default (file, ...path) => new Store(parse(readFileSync(join(process.cwd(), ...path, file), 'utf-8')));
//# sourceMappingURL=getDatasetFromResource.js.map