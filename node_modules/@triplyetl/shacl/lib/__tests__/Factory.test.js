import chai from 'chai';
import { Factory } from '../index.js';
import { Store } from '@triplydb/data-factory';
import Validator from '../Validator.js';
import RulesEngine from '../RulesEngine.js';
import Writer from '../Validator/Report/Writer.js';
import { getValidationReport } from '../Validator/Report/__tests__/Writer.test.js';
describe('Factory', () => {
    it('Validator', () => {
        chai.assert.isTrue(Validator.isValidator(Factory.validator(new Store())));
    });
    it('RulesEngine', () => {
        chai.assert.isTrue(RulesEngine.isRulesEngine(Factory.rulesEngine(new Store())));
    });
    it('SHACL Report Writer', () => {
        const { dataset } = getValidationReport(true);
        chai.assert.isTrue(Writer.isWriter(Factory.reportWriter(dataset)));
    });
});
//# sourceMappingURL=Factory.test.js.map