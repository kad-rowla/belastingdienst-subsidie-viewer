import { QueryEngine } from "./QueryEngine.js";
import { Executor, QueryOptions } from "./Execution.js";
import { Term } from "./constants.js";
import { Store } from "@triplydb/data-factory";
type UpdateResult = typeof updateSuccess;
declare const updateSuccess: "success";
export declare class SparqlEngine extends QueryEngine {
    readonly store: Store;
    constructor(executor: Executor<Term>, opts: {
        store: Store;
        optimize: boolean;
    });
    update(updateString: string, opts?: Omit<QueryOptions, "rdfDataset">): Promise<UpdateResult>;
}
export {};
//# sourceMappingURL=SparqlEngine.d.ts.map