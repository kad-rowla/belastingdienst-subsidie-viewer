import { createRequire } from "module";
// We can expose the version in a cleaner fashion (using ESM imports) when import assertions are properly supported
const require = createRequire(import.meta.url);
const versions = {
  "recognized-datatypes": require("../package.json").version,
};
export default versions;
